{
  "name": "esu-admin",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "deploy": "pnpm run build && sudo docker build -t esu-admin ."
  },
  "dependencies": {
    "@preact/signals": "^2.7.0",
    "preact": "^10.27.2",
    "preact-iso": "^2.11.1"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.2",
    "@types/node": "^24.10.1",
    "@types/react": "^19.2.13",
    "@types/react-dom": "^19.2.3",
    "typescript": "~5.9.3",
    "vite": "^7.2.4"
  }
}
